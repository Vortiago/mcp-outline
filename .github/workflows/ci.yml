name: CI

on:
  push:
    branches: ["**"]
  pull_request:
    branches: [main]

permissions:
  checks: write

jobs:
  test:
    name: Test Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]

    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install uv
        uses: astral-sh/setup-uv@6ee6290f1cbc4156c0bdd66691b2c144ef8df19a # v7
        with:
          enable-cache: true

      - name: Install dependencies
        run: |
          uv sync --group dev

      - name: Check formatting with ruff
        run: |
          uv run ruff format --check .

      - name: Lint with ruff
        run: |
          uv run ruff check .

      - name: Type check with pyright
        run: |
          uv run pyright src/

      - name: Run tests with pytest
        run: |
          uv run pytest tests/ -v -m "not e2e" --cov=src/mcp_outline --cov-report=term --junit-xml=test-results.xml
        env:
          OUTLINE_API_KEY: "dummy"

      - name: Test report
        uses: mikepenz/action-junit-report@49b2ca06f62aa7ef83ae6769a2179271e160d8e4 # v6
        if: always()
        with:
          report_paths: test-results.xml
          check_name: Unit Tests (Python ${{ matrix.python-version }})
